[project]
name = "toponecashier"
version = "0.1.0"
description = "Aplikasi kasir desktop untuk toko/warung kecil"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8.0",
    "pyqt6>=6.6.0",
    "python-escpos>=3.0",
]

[dependency-groups]
dev = [
    "pytest>=7.4.0",
    "pytest-qt>=4.2.0",
]
